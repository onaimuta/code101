"""

; runs next command regardless of results
&& runs the next command if only if the 1st command succeeded
& starts 1st command and starts the 2nd command regardless of results
|| runs the next the command only if the 1st command files

"""